# Auth platform team notes

- Rotation TTLs are owned by the auth platform team; ask in #auth-platform before changing them.
- Session smoke checks must be re-run after any auth store change.
- The public contract for /session/rotate is reviewed by the API guild before release.
- Dashboards that count rotation failures live in the platform console, not in this repository.
